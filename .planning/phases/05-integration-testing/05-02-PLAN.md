---
phase: 05-integration-testing
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - README.md
autonomous: true
requirements:
  - DEMO_PREP
  - DOCUMENTATION
must_haves:
  truths:
    - "README reflects actual project state (CesiumJS, not ArcGIS; all 5 phases complete)"
    - "npm run build produces deployable dist/ without errors"
    - "All tests pass (123 existing + Phase 5 integration tests)"
    - "ROADMAP Phase 5 marked complete"
  artifacts:
    - path: "README.md"
      provides: "Updated project documentation for demo/judges"
    - path: "dist/"
      provides: "Production build output"
  key_links:
    - from: "README.md"
      to: "package.json scripts"
      via: "documented commands match actual scripts"
      pattern: "npm run dev|npm run build|npm run test"
---

<objective>
Update README for demo presentation, verify production build, run full test suite, and mark Phase 5 complete in ROADMAP.

Purpose: Per D-27 (polish & hardening) — make the project presentable to hackathon judges. README is the first thing they see.

Output: Updated README.md, verified dist/ build, all tests green, ROADMAP finalized.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-integration-testing/05-CONTEXT.md
@.planning/phases/05-integration-testing/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README and verify build</name>
  <files>README.md</files>
  <action>
Replace the entire README.md with an updated version that reflects the completed project. Structure:

**# Gemma 4 Autonomous Drone Simulation**

Opening paragraph: Browser-based simulation of an autonomous drone surveying palm oil plantations. Built for the 48-hour hackathon. Combines CesiumJS geospatial mapping, Three.js 3D visualization, and Gemma 4 AI for real-time decision-making.

**## Features**
- 🗺️ **Interactive Area Selection** — Draw rectangles on a CesiumJS globe to define plantation boundaries
- 🌴 **Deterministic Plantation Generation** — Seeded PRNG creates identical layouts from identical selections
- 🚁 **Automated Drone Sweep** — Lawnmower/boustrophedon pattern with terrain-aware altitude
- 🤖 **AI-Powered Decisions** — Gemma 4 analyzes per-waypoint perception data (tree health, ripeness) to modify altitude, flag anomalies, or adjust priorities
- 🌫️ **Fog-of-War Heatmap** — Progressive reveal showing scanned vs unscanned areas
- 📍 **Color-Coded Path Trail** — Visual trail with scan/transit/anomaly markers
- 📊 **AI Reasoning Panel** — Collapsible panel showing structured perception→decision→rationale entries
- 🎥 **Auto-Follow Camera** — Smooth lerp camera tracking the drone through the 3D scene
- 🔄 **Graceful Degradation** — If AI API unavailable, contextual mock decisions keep the demo running

**## Quick Start**
```bash
git clone <repo-url>
cd palm-oil-simulation
npm install
npm run dev
```
Open the URL shown in terminal (typically http://localhost:5173).

**## Environment Variables**
Copy `.env.example` to `.env.local`:
```bash
cp .env.example .env.local
```

| Variable | Required | Purpose |
|----------|----------|---------|
| `VITE_CESIUM_ION_TOKEN` | No | Enables CesiumJS globe with terrain (free tier at cesium.com/ion) |
| `VITE_GEMMA_API_URL` | No | REST endpoint for Gemma 4 inference (Modal/custom) |
| `VITE_GEMMA_API_KEY` | No | Auth key for Gemma API |

The app runs fully without any env vars — CesiumJS shows a placeholder panel, AI uses contextual mock decisions.

**## Architecture**

```
src/
├── app/                 Shell orchestrator + reasoning panel UI
├── config/              Environment configuration
├── map/                 CesiumJS map + rectangle selection controller
├── services/            Gemma API client, perception builder, prompt builder, decision engine
├── simulation/          State manager, sweep path, terrain sampling, AI waypoint loop
├── three/               3D scene controller + helpers (heatmap, path trail, plantation mesh)
├── types/               JSDoc type contracts (selection, plantation, route, AI)
├── world/               Deterministic plantation generator + seeded PRNG
└── styles/              Global CSS
```

**Data Flow:** User draws selection → plantation generated deterministically → sweep path computed → drone animates along waypoints → at each scan point: perception built → AI inference (real or mock) → decision applied → heatmap revealed → trail extended.

**## Tech Stack**
- **Build:** Vite 5
- **3D:** Three.js
- **Maps:** CesiumJS
- **AI:** Gemma 4 via REST API (with mock fallback)
- **Tests:** Vitest (123+ unit tests + integration suite)
- **Language:** Vanilla JavaScript (ES modules, JSDoc types)

**## Scripts**
| Command | Description |
|---------|-------------|
| `npm run dev` | Start dev server with hot reload |
| `npm run build` | Production build to `dist/` |
| `npm run preview` | Preview production build locally |
| `npm test` | Run all tests |
| `npm run test:watch` | Run tests in watch mode |

**## Demo Flow**
1. Open the app — CesiumJS globe (or placeholder) loads with 3D scene panel
2. Draw a rectangle on the map to select a plantation area
3. Click **Start Simulation** — plantation generates, drone begins sweep
4. Watch the AI reasoning panel for real-time decisions
5. Observe fog-of-war heatmap revealing scanned areas
6. Drone path shows color-coded trail with anomaly markers
7. Simulation completes with full coverage — click **Reset** to try again

**## License**
See [LICENSE](./LICENSE).

After writing README, verify the build:
- Run `npm run build` and confirm it exits 0 with `dist/` populated
- Run `npx vitest run` and confirm all tests pass (existing 123 + new Phase 5 tests)
  </action>
  <verify>
    <automated>cd /Users/sanggusti/Workbench/local_development/palm-oil-simulation && npm run build 2>&1 | tail -5 && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>
    - README.md updated with complete project overview, setup instructions, architecture, tech stack, demo flow
    - README references CesiumJS (not ArcGIS), all features from phases 2-4, correct env vars
    - `npm run build` succeeds with dist/ output
    - All tests pass (123 existing + Phase 5 integration tests)
  </done>
</task>

<task type="auto">
  <name>Task 2: Finalize ROADMAP and STATE</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
Update `.planning/ROADMAP.md` Phase 5 section:

1. Add after the "Objective" line:
   ```
   **Goal:** End-to-end integration testing, performance verification, and demo-ready polish
   **Requirements:** E2E_TESTING, STABILITY, PERFORMANCE, DEMO_PREP, DOCUMENTATION
   **Plans:** 2 plans

   Plans:
   - [x] 05-01-PLAN.md — E2E integration tests, state lifecycle, graceful degradation, performance
   - [x] 05-02-PLAN.md — README update, build verification, ROADMAP finalization
   ```

2. Mark all Phase 5 tasks with checkmarks in the Tasks list.

Update `.planning/STATE.md`:
- Set `status: complete`
- Set `completed_phases: 5`
- Set `completed_plans: 14` (12 prior + 2 new)
- Update Active Phase to "Phase 5: Integration & Testing — COMPLETE"
- Add Phase 5 milestones to the Current Milestones list:
  - `[x] Phase 5 Plan 05-01: E2E Integration Tests completed`
  - `[x] Phase 5 Plan 05-02: Demo Prep & README completed`
  - `[x] Phase 5: Integration & Testing COMPLETE (2/2 plans)`
  - `[x] PROJECT COMPLETE — Demo Ready`
  </action>
  <verify>
    <automated>cd /Users/sanggusti/Workbench/local_development/palm-oil-simulation && grep -c "\[x\] 05-0" .planning/ROADMAP.md && grep "status:" .planning/STATE.md</automated>
  </verify>
  <done>
    - ROADMAP Phase 5 has plan list with both plans checked off
    - STATE.md shows status: complete, 5/5 phases, 14/14 plans
    - Project is fully documented as complete and demo-ready
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README → User | Public-facing documentation — no secrets exposed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | I (Information Disclosure) | README.md | mitigate | Do not include any API keys, tokens, or internal URLs in README. Reference `.env.example` only. |
| T-05-04 | I (Information Disclosure) | dist/ | accept | Production build is public-facing by design; no secrets bundled (env vars are build-time only) |
</threat_model>

<verification>
```bash
# Full test suite passes
npx vitest run

# Build succeeds
npm run build

# README has correct content
grep -q "CesiumJS" README.md && echo "CesiumJS mentioned: OK"
grep -q "Gemma 4" README.md && echo "Gemma 4 mentioned: OK"
grep -q "npm run dev" README.md && echo "Dev command documented: OK"

# ROADMAP finalized
grep "05-01-PLAN" .planning/ROADMAP.md
grep "05-02-PLAN" .planning/ROADMAP.md
```
</verification>

<success_criteria>
- README.md is a complete, accurate project overview suitable for hackathon judges
- `npm run build` exits 0 with populated dist/
- All tests pass (full suite)
- ROADMAP and STATE reflect project completion
- No secrets or internal details exposed in README
</success_criteria>

<output>
After completion, create `.planning/phases/05-integration-testing/05-02-SUMMARY.md`
</output>
